<Window
    x:Class="Gress.Demo.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="clr-namespace:Gress.Demo.ViewModels"
    Title="Gress Demo"
    Width="500"
    Height="400"
    Background="{DynamicResource MaterialDesignPaper}"
    Icon="/Gress.Demo;component/favicon.ico"
    Style="{DynamicResource MaterialDesignRoot}"
    WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewModels:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Aggregated progress  -->
        <GroupBox
            Grid.Row="0"
            Margin="8"
            Header="Overall">
            <StackPanel Orientation="Vertical">
                <!--  Overview  -->
                <TextBlock>
                    <Run Text="Operations:" />
                    <Run Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding Operations.Count, Mode=OneWay}" />
                    <Run />
                    <Run Text="Progress:" />
                    <Run Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding Progress.Current, Mode=OneWay}" />
                </TextBlock>

                <!--  Progress  -->
                <ProgressBar
                    Height="4"
                    Margin="0,16,0,0"
                    Value="{Binding Progress.Current.Fraction, Mode=OneWay}" />
            </StackPanel>
        </GroupBox>

        <!--  Operations  -->
        <GroupBox
            Grid.Row="1"
            Margin="8"
            Header="Operations">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsControl HorizontalContentAlignment="Stretch" ItemsSource="{Binding Operations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!--  Overview  -->
                                <TextBlock
                                    Grid.Column="0"
                                    Margin="0,2,0,0"
                                    Foreground="{DynamicResource SecondaryTextBrush}">
                                    <Run Text="Weight:" />
                                    <Run Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding Weight, Mode=OneWay}" />
                                    <Run />
                                    <Run Text="Progress:" />
                                    <Run Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding Progress.Current, Mode=OneWay}" />
                                </TextBlock>

                                <!--  Progress  -->
                                <ProgressBar
                                    Grid.Column="2"
                                    Background="#0F000000"
                                    Foreground="{DynamicResource SecondaryAccentBrush}"
                                    Maximum="1"
                                    Minimum="0"
                                    Style="{StaticResource MaterialDesignCircularProgressBar}"
                                    Value="{Binding Progress.Current.Fraction, Mode=OneWay}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!--  New operation  -->
        <Grid Grid.Row="2" Margin="8,16,8,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Weight  -->
            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <TextBlock FontSize="15">
                    <Run Text="Weight:" />
                    <Run Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding Value, ElementName=WeightSlider, Mode=OneWay}" />
                </TextBlock>
                <Slider
                    x:Name="WeightSlider"
                    Width="200"
                    Margin="24,0,0,0"
                    HorizontalAlignment="Left"
                    IsSnapToTickEnabled="True"
                    Maximum="10"
                    Minimum="1"
                    TickFrequency="1" />
            </StackPanel>

            <!--  Start button  -->
            <Button
                Grid.Column="2"
                Command="{Binding PerformWorkCommand}"
                CommandParameter="{Binding Value, ElementName=WeightSlider}"
                Content="START OPERATION" />
        </Grid>
    </Grid>
</Window>